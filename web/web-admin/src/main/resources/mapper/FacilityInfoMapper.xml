<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.greatxcf.lease.web.admin.mapper.FacilityInfoMapper">

    <select id="selectListByApartmentId" resultType="com.greatxcf.lease.model.entity.FacilityInfo">
        select id, type, name, icon
        from lease.facility_info
        where is_deleted = 0
          and id in (select facility_id
                     from lease.apartment_facility
                     where is_deleted = 0
                       and apartment_id = #{id})
    </select>

    <select id="selectListByRoomId" resultType="com.greatxcf.lease.model.entity.FacilityInfo">
        select id, type, name, icon
        from lease.facility_info
        where id in (select facility_id
                     from lease.room_facility
                     where room_id = #{id}
                       and room_facility.is_deleted = 0)
          and is_deleted = 0
    </select>
</mapper>
