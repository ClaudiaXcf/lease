<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.greatxcf.lease.web.admin.mapper.LeaseAgreementMapper">

    <resultMap id="AgreementVoMap" type="com.greatxcf.lease.web.admin.vo.agreement.AgreementVo" autoMapping="true">
        <id property="id" column="id"/>
        <association property="apartmentInfo" javaType="com.greatxcf.lease.model.entity.ApartmentInfo" autoMapping="true">
            <id property="id" column="id"/>
            <result property="id" column="apart_id"/>
            <result property="name" column="apart_name"/>
        </association>
        <association property="roomInfo" javaType="com.greatxcf.lease.model.entity.RoomInfo" autoMapping="true">
            <id property="id" column="room_id"/>
            <result property="roomNumber" column="room_number"/>
            <result property="rent" column="ri_rent"/>
            <result property="apartmentId" column="ri_apartment_id"/>
            <result property="isRelease" column="is_release"/>
        </association>
        <association property="paymentType" javaType="com.greatxcf.lease.model.entity.PaymentType" autoMapping="true">
            <id property="id" column="payment_type_id"/>
            <result property="name" column="payment_type_name"/>
        </association>
        <association property="leaseTerm" javaType="com.greatxcf.lease.model.entity.LeaseTerm" autoMapping="true">
            <id property="id" column="lease_term_id"/>
        </association>
    </resultMap>

    <select id="pageItem" resultMap="AgreementVoMap">
        select la.id,
               la.phone,
               la.name,
               la.identification_number,
               la.apartment_id,
               la.room_id,
               la.lease_start_date,
               la.lease_end_date,
               la.lease_term_id,
               la.rent,
               la.deposit,
               la.payment_type_id,
               la.status,
               la.source_type,
               la.additional_info,
               ai.id    apart_id,
               ai.name  apart_name,
               ai.introduction,
               ai.district_id,
               ai.district_name,
               ai.city_id,
               ai.city_name,
               ai.province_id,
               ai.province_name,
               ai.address_detail,
               ai.latitude,
               ai.longitude,
               ai.phone apart_phone,
               ri.id    room_id,
               ri.room_number,
               ri.rent   ri_rent,
               ri.apartment_id ri_apartment_id,
               ri.is_release,
               pt.id    payment_type_id,
               pt.name  payment_type_name,
               pt.pay_month_count,
               pt.additional_info,
               lt.id    lease_term_id,
               lt.month_count,
               lt.unit
        from lease.lease_agreement la
                 left join lease.apartment_info ai
                           on la.apartment_id = ai.id and ai.is_deleted = 0
                 left join lease.room_info ri
                           on la.room_id = ri.id and ri.is_deleted = 0
                 left join lease.payment_type pt
                           on la.payment_type_id = pt.id and pt.is_deleted = 0
                 left join lease.lease_term lt
                           on la.lease_term_id = lt.id and lt.is_deleted = 0
        <where>
            la.is_deleted = 0
            <if test="queryVo.provinceId != null">
                and ai.province_id = #{queryVo.provinceId}
            </if>
            <if test="queryVo.cityId != null">
                and ai.city_id = #{queryVo.cityId}
            </if>
            <if test="queryVo.districtId != null">
                and ai.district_id = #{queryVo.districtId}
            </if>
            <if test="queryVo.apartmentId != null">
                and la.apartment_id = #{queryVo.apartmentId}
            </if>
            <if test="queryVo.roomNumber != null and queryVo.roomNumber != ''">
                and ri.room_number = #{queryVo.roomNumber}
            </if>
            <if test="queryVo.name != null and queryVo.name != ''">
                and la.name like concat('%',#{queryVo.name},'%')
            </if>
            <if test="queryVo.phone != null and queryVo.phone != ''">
                and la.phone like concat('%',#{queryVo.phone},'%')
            </if>
        </where>
    </select>
</mapper>
