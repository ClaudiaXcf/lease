<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.greatxcf.lease.web.admin.mapper.ViewAppointmentMapper">

    <resultMap id="AppointmentVoMap" type="com.greatxcf.lease.web.admin.vo.appointment.AppointmentVo" autoMapping="true">
        <id property="id" column="id"/>
        <association property="apartmentInfo" javaType="com.greatxcf.lease.model.entity.ApartmentInfo" autoMapping="true">
            <id property="id" column="appoint_id"/>
            <result property="name" column="appoint_name"/>
            <result property="phone" column="appoint_phone"/>
        </association>
    </resultMap>

    <select id="pageAppointmentVo" resultMap="AppointmentVoMap">
        select va.id,
               va.user_id,
               va.name,
               va.phone,
               va.apartment_id,
               va.appointment_time,
               va.additional_info,
               va.appointment_status,
               ai.id appoint_id,
               ai.name appoint_name,
               ai.introduction,
               ai.district_id,
               ai.district_name,
               ai.city_id,
               ai.city_name,
               ai.province_id,
               ai.province_name,
               ai.address_detail,
               ai.latitude,
               ai.longitude,
               ai.phone appoint_phone,
               ai.is_release
        from lease.view_appointment va
                 left join lease.apartment_info ai
                           on ai.is_deleted = 0 and ai.id = va.apartment_id
        <where>
            va.is_deleted = 0
            <if test="queryVo.provinceId != null">
                and ai.province_id = #{queryVo.provinceId}
            </if>
            <if test="queryVo.cityId != null">
                and ai.city_id = #{queryVo.cityId}
            </if>
            <if test="queryVo.districtId != null">
                and ai.district_id = #{queryVo.districtId}
            </if>
            <if test="queryVo.apartmentId != null">
                and va.apartment_id = #{queryVo.apartmentId}
            </if>
            <if test="queryVo.name != null and queryVo.name != ''">
                and va.name like contact ('%',#{queryVo.name},''%)
            </if>
            <if test="queryVo.phone != null and queryVo.phone != ''">
                and va.phone like contact('%',#{queryVo.phone},''%)
            </if>
        </where>
    </select>
</mapper>
