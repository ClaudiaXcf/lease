<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.greatxcf.lease.web.admin.mapper.AttrValueMapper">

    <select id="selectListByRoomId" resultType="com.greatxcf.lease.web.admin.vo.attr.AttrValueVo">
        select av.id,av.name, av.attr_key_id, ak.name attr_key_name
        from lease.attr_value av
                 left join lease.attr_key ak
                           on ak.is_deleted = 0 and
                              av.attr_key_id = ak.id
        where av.id in (select attr_value_id
                        from lease.room_attr_value
                        where room_id = #{id})
        and av.is_deleted = 0
    </select>
</mapper>
